var a = false || 8 || 0;


if(languages==undefined) {
    var languages = [
        'Javascript',
        'Javascript',
        'Javascript',
    ]
}

sessionStorage = languages

languages.push('PHP', 'Dart')

console.log(sessionStorage);